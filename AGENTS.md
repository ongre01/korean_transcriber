# Korean Transcriber 에이전트 안내

## 프로젝트 목적

이 프로젝트는 무료 또는 로컬에서 실행할 수 있는 AI 모델을 이용해 녹음 파일을 한국어로 전사하는 프로젝트다. 로컬 실행을 우선해 사용자의 녹음 데이터와 전사 내용을 외부 서비스로 보내지 않고, 반복적인 API 비용 없이 사용할 수 있도록 하는 것을 기본 방향으로 삼는다.

현재 구현은 범용 텍스트 생성 LLM이 아니라 음성 인식에 특화된 OpenVINO Whisper Large-v3-Turbo ASR 모델을 사용한다. INT8과 FP16 모델을 Intel NPU에서 로컬로 실행하며, 전사 결과는 입력 파일과 같은 위치에 TXT 및 SRT 형식으로 저장한다.

## 개발 원칙

- 로컬 실행, 무료 사용을 우선한다.
- 사용자가 명시적으로 요청하지 않는 한 유료 API나 녹음 데이터를 외부로 전송하는 클라우드 서비스를 추가하지 않는다.
- 동작이나 문서를 변경할 때는 실제 모델명, 실행 명령, 출력 형식이 현재 코드와 일치하는지 확인한다.

## 주요 진입점

- `setup.bat`: Python 가상 환경과 필수 패키지를 준비하고 모델을 내려받는다.
- `run_compare.bat`: 하나의 녹음 파일을 모델별로 실행한 결과를 비교하기 위해 각각의 차례대로 전사해 결과를 비교한다.
- `transcribe_npu.py`: 오디오 디코딩, OpenVINO Whisper 실행, TXT/SRT 저장을 담당하는 핵심 전사 프로그램이다.

사용 방법과 현재 모델 구성에 관한 추가 정보는 `README_KO.md`를 참고한다.
e